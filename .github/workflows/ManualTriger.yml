name: Manaual Triger

on:
  workflow_dispatch:
 inputs:
 name:
 description: 'Name'
 type: String
 default: 'Test'
    
  push:
    branches:
      - main

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Greetings
        run: echo "Hello, world"
